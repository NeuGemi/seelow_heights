GER_oberkommando = {
	GER_hold_holland = {
		icon = generic_civil_support 

		available = {
			HOL = {
				NOT = { controls_state = 7 }	
			}
		}

		fire_only_once = yes
		is_good = yes
		days_mission_timeout = 60
		allowed = {
			tag = GER
		}
		activation = {
			HOL = {
				controls_state = 7			
			}		
		}

		visible = {
			tag = GER
			has_war = yes
		}

		complete_effect = {
		}		
	}
	GER_hold_vienna = {

		icon = generic_civil_support

		allowed = {
			original_tag = GER
		}

		#Fail trigger
		available = {
			NOT = { controls_state = 4 }
		}

		days_mission_timeout = 20

		fire_only_once = yes

		is_good = yes

		activation = {
			has_war = yes
			has_civil_war = no
			4 = {
				is_controlled_by = ROOT
				OR = {
					any_neighbor_state = {
						CONTROLLER = {
							has_war_with = ROOT
						}
						OWNER = { tag = ROOT }
					}
					NOT = { is_fully_controlled_by = ROOT }
				}
			}
		}

		#Success
		timeout_effect = {
			add_stability = 0.05
			add_war_support = 0.05
			#country_event = wtt_soviet_hold_city.1
		}
		#Fail
		complete_effect = {
			#country_event = wtt_soviet_hold_city.2
		}
	}
	GER_hold_berlin = {

		icon = generic_civil_support

		allowed = {
			original_tag = GER
		}

		#Fail trigger
		available = {
			NOT = { controls_state = 64 }
		}

		days_mission_timeout = 45

		fire_only_once = yes

		is_good = yes

		activation = {
			has_war = yes
			has_civil_war = no
			64 = {
				is_controlled_by = ROOT
				OR = {
					any_neighbor_state = {
						CONTROLLER = {
							has_war_with = ROOT
						}
						OWNER = { tag = ROOT }
					}
					NOT = { is_fully_controlled_by = ROOT }
				}
			}
		}

		#Success
		timeout_effect = {
			add_stability = 0.05
			add_war_support = 0.05
			#country_event = wtt_soviet_hold_city.1
		}
		#Fail
		complete_effect = {
			#country_event = wtt_soviet_hold_city.2
		}
	}
	GER_hold_stuttgart = { #Heilbronn

		icon = generic_civil_support

		allowed = {
			original_tag = GER
		}

		#Fail trigger
		available = {
			NOT = { controls_province = 3530 }
		}

		days_mission_timeout = 20

		fire_only_once = yes

		is_good = yes

		activation = {
			has_war = yes
			has_civil_war = no
			50 = {
				is_controlled_by = ROOT
				OR = {
					any_neighbor_state = {
						CONTROLLER = {
							has_war_with = ROOT
						}
						OWNER = { tag = ROOT }
					}
					NOT = { is_fully_controlled_by = ROOT }
				}
			}
		}

		#Success
		timeout_effect = {
			add_stability = 0.05
			add_war_support = 0.05
			#country_event = wtt_soviet_hold_city.1
		}
		#Fail
		complete_effect = {
			#country_event = wtt_soviet_hold_city.2
		}
	}
	GER_hold_italy = { #Operation Grapeshot

		icon = generic_civil_support

		allowed = {
			original_tag = GER
		}

		#Fail trigger
		available = {
			ITA = {
				NOT = { controls_state = 159 }
			}
		}

		days_mission_timeout = 60

		fire_only_once = yes

		is_good = yes

		activation = {
			has_war = yes
			159 = {
				is_controlled_by = ITA
			}
		}

		#Success
		timeout_effect = {
			add_stability = 0.05
			add_war_support = 0.05
		}
		#Fail
		complete_effect = {
		}
	}
}

JAP_hold_territory = {
		JAP_hold_burma = {

		icon = generic_civil_support

		allowed = {
			original_tag = JAP
		}

		#Fail trigger
		available = {
			NOT = { controls_state = 288 }
		}

		days_mission_timeout = 95

		fire_only_once = yes

		is_good = yes

		activation = {
			has_war = yes
			has_civil_war = no
			288 = {
				is_controlled_by = ROOT
				OR = {
					any_neighbor_state = {
						CONTROLLER = {
							has_war_with = ROOT
						}
						OWNER = { tag = ROOT }
					}
					NOT = { is_fully_controlled_by = ROOT }
				}
			}
		}

		#Success
		timeout_effect = {
			add_stability = 0.03
			add_war_support = 0.03
			#country_event = wtt_soviet_hold_city.1
		}
		#Fail
		complete_effect = {
			#country_event = wtt_soviet_hold_city.2
		}
	}
		JAP_hold_phi = {

		icon = generic_civil_support

		allowed = {
			original_tag = JAP
		}

		#Fail trigger
		available = {
			NOT = { controls_state = 627 controls_state = 623 }
		}

		days_mission_timeout = 90

		fire_only_once = yes

		is_good = yes

		activation = {
			has_war = yes
			has_civil_war = no
			627 = {
				is_controlled_by = ROOT
				OR = {
					any_neighbor_state = {
						CONTROLLER = {
							has_war_with = ROOT
						}
						OWNER = { tag = ROOT }
					}
					NOT = { is_fully_controlled_by = ROOT }
				}
			}
			623 = {
				is_controlled_by = ROOT
				OR = {
					any_neighbor_state = {
						CONTROLLER = {
							has_war_with = ROOT
						}
						OWNER = { tag = ROOT }
					}
					NOT = { is_fully_controlled_by = ROOT }
				}
			}
		}

		#Success
		timeout_effect = {
			add_stability = 0.05
			add_war_support = 0.05
			#country_event = wtt_soviet_hold_city.1
		}
		#Fail
		complete_effect = {
			#country_event = wtt_soviet_hold_city.2
		}
	}
		JAP_hold_okinawa = {

		icon = generic_civil_support

		allowed = {
			original_tag = JAP
		}

		#Fail trigger
		available = {
			NOT = { controls_province = 11932 controls_province = 10124 controls_province = 7115 }
		}

		days_mission_timeout = 35

		fire_only_once = yes

		is_good = yes

		activation = {
			has_war = yes
			has_civil_war = no
		}

		#Success
		timeout_effect = {
			add_stability = 0.05
			add_war_support = 0.05
			#country_event = wtt_soviet_hold_city.1
		}
		#Fail
		complete_effect = {
			#country_event = wtt_soviet_hold_city.2
		}
	}
}

scenario_settings = {
	GER_scenario_settings = {
		icon = decision_generic_human_torpedo 

		available = {
			tag = GER
			NOT = {
				is_ai = yes
			}
		}

		allowed = {
			tag = GER
		}

		visible = {
			tag = GER
		}

		fire_only_once = yes

		complete_effect = {
			remove_ideas = GER_shattered_wehrmacht
			remove_ideas = GER_endsiegmod
			remove_ideas = GER_totalen_krieg_doctrine
			remove_ideas = GER_economic_collapse
		}		
	}	
	JAP_scenario_settings = {
		icon = decision_generic_human_torpedo 

		available = {
			tag = JAP
			NOT = {
				is_ai = yes
			}
		}

		allowed = {
			tag = JAP
		}

		visible = {
			tag = JAP
		}

		fire_only_once = yes

		complete_effect = {
			remove_ideas = JAP_shattered_army
			remove_ideas = JAP_economic_collapse
		}		
	}	
}